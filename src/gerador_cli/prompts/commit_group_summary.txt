# Prompt: Resumir um grupo de commits em bullets.

You need to summarize a compilation of "{{change_type_label}}" type changes.

Create a list of bullet points that summarizes all the changes made by the commits (1-2 sentences per commit).

Be clear and direct, providing enough detail to understand the changes without omitting details, but without adding or deducting information. Use only the list of commits provided to generate the summary.

Keep the order of the commits, as they are sorted by level of importance.

The final text should be in {{language_hint}}

Return only the list of bullet points in the requested format.

Commit list:
{{commit_blocks}}
