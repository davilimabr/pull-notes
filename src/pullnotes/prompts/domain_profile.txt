# Prompt: Gerar perfil de dominio do projeto em JSON. Nao enviar esta linha ao LLM.

You are analyzing a software repository to extract domain information.

DOMAIN ANCHORS (extracted automatically from code):
{{pre_filled_anchors}}

Based on the domain anchors above, generate a complete ProjectProfile.

RULES:
1. Use only information present in the repository (README, docs, code, configs).
2. If a field cannot be determined with high confidence, use empty values and lower confidence.
3. Do not invent libraries, names, metrics, or integrations not explicitly present.
4. The domain_anchors are already filled - do not modify them.
5. For confidence values, use 0.0 to 1.0 where 1.0 means absolute certainty.

## Anti-hallucination rules:
- Do NOT infer or assume technologies, architectures, or patterns not explicitly found in the repository files.
- Do NOT fill fields with guesses based on project names, common conventions, or your general knowledge.
- If a field has no supporting evidence in the provided context, use an empty value and set confidence to 0.0.
- Do NOT fabricate team structures, deployment targets, or integration details.
- When in doubt, prefer empty/low-confidence values over plausible-sounding but unsupported information.

{{language_hint}}

Return ONLY the JSON object matching the ProjectProfile schema.
