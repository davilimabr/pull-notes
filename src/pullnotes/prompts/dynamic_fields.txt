# Prompt: Gerar campos dinamicos para PR ou Release a partir de secoes do template.

You are a careful assistant generating {{template_type}} descriptions.
Based on the commit summaries and change details below, produce a JSON object with the fields described.

{{language_hint}}

## Anti-hallucination rules:
- Use ONLY information explicitly present in the commit summaries and change details below.
- Do NOT invent, assume, or infer details not stated in the data.
- Do NOT add context from your own knowledge about common risks, best practices, or typical patterns.
- Only return an empty string for a field if there is truly NO relevant data in the commits for that field.

## Fields to generate:
Each field corresponds to a section in the output document. The description explains what content is expected.

{{sections_block}}

## Context data:
{{extra_context}}
### Commit summaries:
{{commit_summaries}}

### Detailed changes by type (MUST be distributed across the fields above):
{{changes_by_type}}

## CRITICAL RULES:
1. The detailed changes listed above are for CONTEXT ONLY — they are already placed in a dedicated "Changes" section automatically. Do NOT copy or repeat them in the fields you generate.
2. Use the commit summaries and change details to INFORM your writing (e.g., to describe the problem/solution, identify the type of change, assess risks), but do NOT list the individual changes again.
3. Each field should contain original, synthesized content appropriate for that section — not a copy of the changes list.
4. Do NOT leave fields empty if there is relevant information in the commits for that field.

{{alerts_block}}

IMPORTANT: Return ONLY a JSON object with this exact structure (do not include any other text):
{{json_example}}

{{language_reminder}}
