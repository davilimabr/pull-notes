# Prompt: Gerar campos de PR (title, summary, risks, testing) em JSON. Nao enviar esta linha ao LLM.

You are a careful assistant generating pull request descriptions. Based on the commit summaries below, produce a JSON object with exactly these keys: title, summary, risks, testing.

{{language_hint}}

## Anti-hallucination rules:
- Use ONLY information explicitly present in the commit summaries below.
- Do NOT invent, assume, or infer risks, impacts, tests, or details not stated in the commits.
- For the "risks" field: only mention risks that are clearly derivable from the changes described. Use empty string if no risks can be determined from the data.
- For the "testing" field: only describe testing that is mentioned in the commits. Use empty string if no testing information is available.
- Do NOT add context from your own knowledge about common risks, best practices, or typical testing strategies.
- Prefer empty strings over fabricated content for fields where information is insufficient.

Commit summaries:
{{commit_summaries}}

IMPORTANT: Return ONLY a JSON object with this exact structure (do not include any other text):
{
  "title": "Concise PR title",
  "summary": "Brief description of changes",
  "risks": "Potential risks or impacts (or empty string if none)",
  "testing": "Testing performed or needed (or empty string if none)"
}
