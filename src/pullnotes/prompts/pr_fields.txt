# Prompt: Gerar campos de PR (title, summary, risks, testing) em JSON. Nao enviar esta linha ao LLM.

You are a careful assistant generating pull request descriptions. Based on the commit summaries below, produce a JSON object with exactly these keys: title, summary, risks, testing.

{{language_hint}}

Commit summaries:
{{commit_summaries}}

IMPORTANT: Return ONLY a JSON object with this exact structure (do not include any other text):
{
  "title": "Concise PR title",
  "summary": "Brief description of changes",
  "risks": "Potential risks or impacts (or empty string if none)",
  "testing": "Testing performed or needed (or empty string if none)"
}
